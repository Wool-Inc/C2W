/*
package net.klaaswhite.c2w.interfaces;

import net.klaaswhite.c2w.classes.ManagedPlayer;
import net.klaaswhite.c2w.managers.GameManager;
import org.bukkit.entity.Item;
import org.bukkit.event.entity.EntityDeathEvent;
import org.bukkit.event.entity.EntityPickupItemEvent;
import org.bukkit.event.inventory.InventoryClickEvent;
import org.bukkit.event.player.*;

public interface ISpecie {
    // Lifetime Events
    void onApply(GameManager manager, ManagedPlayer p);

    void onRemove();

    // Events

    // Player lifecycle
    void onRespawn(PlayerRespawnEvent playerRespawnEvent);

    void onDeath();

    // Damage
    void onHit();

    void onDamage(int amount);

    void onProvideHit(ManagedPlayer target);

    void onKillEntity(EntityDeathEvent entityDeathEvent);

    // Inventory related
    void onDropItem(Item droppedItem);

    void onSwapHand(PlayerSwapHandItemsEvent swapHandEvent);

    void onPickupItem(EntityPickupItemEvent pickupItemEvent);

    void onInventoryClick(InventoryClickEvent inventoryClickEvent);

    // Interaction
    boolean onInteractEntity(PlayerInteractEntityEvent playerInteractEntityEvent);

    boolean onInteract(PlayerInteractEvent playerInteractEvent);

    // Movement
    void onToggleSneak(PlayerToggleSneakEvent playerToggleSneakEvent);

}
*/
